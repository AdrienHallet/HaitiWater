/**
 * Custom Table Handler
 * Used to prettify the table and make it respond to custom input and commands
 *
 */
$(document).ready(function() {
    // Draw the water element table without the managers
    drawWaterElementTable(false);
});
